#pragma config(Sensor, dgtl1,  EncoderL,            sensorQuadEncoder)
#pragma config(Sensor, dgtl3,  EncoderR,            sensorQuadEncoder)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

int gearRatio


void PointTurnDegrees (int degree, int direction, int speed)
{
   // Zero out variables and sensors
   int turned = 0;
   int traveled = 0;
   SensorValue[leftEncoder] = 0;
   SensorValue[rightEncoder] = 0;

   // Lets get moving!
   motor[leftMotor] = speed * direction;
   motor[rightMotor] = speed * direction * -1;

   while (turned <= degree)
   {
      DriveStraight();
      traveled = (SensorValue[leftEncoder] + SensorValue[rightEncoder]) * DistancePerTick;
      turned = (traveled / TurningCircle);
      wait1Msec(SleepTime);
   }
}

int distance()
{
  return ((float)(abs(SensorValue[EncoderL]) +
  (float)abs(SensorValue[EncoderR]))  * 4 / 360 * PI);
}
void foward(float ticks, int speed)
{
  while(distance< ticks)
  {
    motor[port1]= speed;
    motor[port2] = speed;
  }
  motor[port1] = 0;
  motor[port2] = 0;
}
task main()
{}
